// Camada base da API (neste momento simulada)
// Futuramente, instancie o Axios ou Fetch estendendo esta classe/objeto.

export const api = {
  // Exemplo de como poderia ser uma chamada verdadeira
  // get: async (endpoint) => fetch(endpoint).then(res => res.json()),
  // post: async (endpoint, data) => fetch(endpoint, { method: 'POST', body: JSON.stringify(data) })
};
